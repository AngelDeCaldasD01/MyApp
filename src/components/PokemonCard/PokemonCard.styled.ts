import { StyleSheet } from 'react-native';

const cardStyles = StyleSheet.create<any>({
    cardContainer: {
        flex: 1,
        height: 130,
        backgroundColor: 'red',
    },
    spacing: {
        flex: 1,
        padding: 5
    }
});

export default cardStyles;
